Put the upright correction model checkpoint here.

Expected default file name:

```text
best_model_eff_b0_bright_global7_residual_fusion_real_best.pth
```

You can also keep the checkpoint somewhere else and set:

```powershell
$env:STEADYVIEW_UPRIGHT_MODEL_PATH="C:\path\to\model.pth"
```
